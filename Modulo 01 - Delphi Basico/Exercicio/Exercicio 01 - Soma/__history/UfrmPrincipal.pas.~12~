unit UfrmPrincipal;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.StdCtrls;

type
  TfrmPrincipal = class(TForm)
    edtNumerador: TEdit;
    lblSaudação: TLabel;
    lblTitulo: TLabel;
    lblSoma: TLabel;
    edtDenominador: TEdit;
    lblIgual: TLabel;
    lblResultado: TLabel;
    btnSomar: TButton;
    lblResultado2: TLabel;
    procedure btnSomarClick(Sender: TObject);
  private
    function Somar(aNumerador, aDenominador: String): String;
  public
    { Public declarations }
  end;

var
  frmPrincipal: TfrmPrincipal;

implementation

{$R *.dfm}

function TfrmPrincipal.Somar(aNumerador, aDenominador: String): string;
  var
    xsoma: Double;
  begin
    try
      xsoma := StrToFloat(aNumerador) + StrToFloat(aDenominador);
      result := xSoma.ToString;
    except
      lblResultado.Caption   := 'Indique somente';
      lblResultado2.Caption  := 'números Reais!';
      lblResultado.Visible   := True;
      lblResultado2.Visible  := True;
    end;
  end;

procedure TfrmPrincipal.btnSomarClick(Sender: TObject);
  begin
    try
      lblResultado.Caption := Somar(edtNumerador.Text, edtDenominador.Text);
      lblResultado.Visible := True;
    except
      lblResultado.Caption   := 'Indique somente';
      lblResultado2.Caption  := 'números Reais!';
      lblResultado.Visible   := True;
      lblResultado2.Visible  := True;
    end;
  end;

end.
